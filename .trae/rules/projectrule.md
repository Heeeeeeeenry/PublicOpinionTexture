这个项目名称叫民意质感系统。
项目采用django框架，数据库采用mysql。主机地址为81.70.230.137，运行的系统为ubuntu，项目需要运行在18081端口，允许外部访问。
mysql数据库的地址为81.70.230.137，端口为53306，用户名为pot，密码为000001，能够读写的数据库名称为pot_data。
项目的html代码、css代码、js代码需要分开，由主界面调用。js代码尽量使用异步函数，等待异步操作完成后再继续执行。
**前端、后端的代码不要写过多的错误捕获，让问题暴露出来，显示在终端上。**
注释详细到函数级。
该项目使用django的RDF规则。
你应该采用tailwindcss框架，来编写html代码。
你应该使用fontawesome字体图标库。
所有的元素尽量采用圆角长方形。
python虚拟环境在项目根目录下，名为venv。
项目的启动和重启你无需管理，在有需要的时候告诉我就行。
创建的代码需要遵从项目设计文档。
新添加的功能需要及时更新项目设计文档和数据库使用手册。
通用动画和通用css存在项目中，你需要尽量使用通用动画和通用样式，如果实在无法满足需求，则你可以创建一个专用文件来满足额外需求。
